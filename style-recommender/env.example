NEXTAUTH_SECRET=          # Must match the finance app's NEXTAUTH_SECRET
DATABASE_URL=file:prisma/dev.db
WORKER_TOKEN=             # Shared secret for the local worker to authenticate with the API
DATA_DIR=                 # Optional: override image storage dir (default: ./data)
